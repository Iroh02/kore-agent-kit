"""Test package.

This file is required, not incidental: without it ``tests`` is only a namespace
portion, so an installed top-level ``tests`` package elsewhere on sys.path wins
and ``python -m tests.smoke`` fails.
"""
